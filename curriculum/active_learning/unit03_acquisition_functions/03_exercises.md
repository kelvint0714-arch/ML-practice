# Unit 03 练习

## 练习 1

候选 A、B、C 的 \((\mu,\sigma)\) 为 `(6,0.2)`、`(5,1.5)`、`(5.8,0.5)`。分别计算 \(\beta=0,1,2\) 的 UCB 排序。

## 练习 2

解释 `best_observed = y_pool.max()` 为什么是泄漏，并写出正确变量。

## 练习 3

为什么 PI 可能喜欢“很可能只提高一点”的候选，而 EI 更重视提高幅度？

## 练习 4

目标是最小化实验损失。写出 LCB，并说明选择 `argmin` 还是 `argmax`。

## 练习 5

两个候选采集分数相同。为什么仍要固定候选 ID 并列规则？

## 练习 6

最大化任务中 `best_observed=6.0`。A 的 \((\mu,\sigma)=(6.2,0.2)\)，
B 的 \((5.9,0.5)\)。先令 `xi=0`，计算 PI/EI 并选择候选。可使用：

- \(\Phi(1)=0.8413,\phi(1)=0.2420\)；
- \(\Phi(-0.2)=0.4207,\phi(-0.2)=0.3910\)。

再说明提高 `xi` 为什么可能改变排序。
