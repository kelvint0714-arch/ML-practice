# Day 13 练习参考答案

## A. 概念题

1. 相同样本、输入信息、验证折、指标、目标空间和预先声明的选择规则。
2. 不必然。不同算法可使用必要预处理；只要预处理不泄漏、输入信息相同且验证协议相同，就是公平的。
3. 这样每个模型面对难度完全相同的验证样本，支持逐折配对比较。
4. 提供不利用特征关系的最低参照，检查复杂模型是否真正增加预测价值。
5. 不能。它只是在当前数据、折和配置中观察到的排名。
6. 同一折既参与参数选择又用于宣称性能，会吸收选择偏差。需要重新声明固定基线、独立评价或嵌套 CV。

## B. 表结构题

7. `5 × 5 = 25` 行。
8. `model` 模型名、`fold` 折号、`split` 证据区域、`mae`、`rmse` 和 `r2`。`fit_seconds` 只保留在运行时内存变量中，不进入预存展示或持久化 fixture。
9. 汇总会隐藏异常折、缺折、重复行和模型间逐折一致性。
10. 不同模型若基于不同数量或不同集合的折，均值不再处于相同证据条件，且缺失可能来自运行失败。

## C. 代码题

11. 变为 6 个模型后应有 30 行：

```python
assert len(fold_metrics) == 6 * 5
```

12. `delta < 0` 表示该模型该折 RMSE 低于 Dummy。可通过 pivot 对齐：

```python
wide = fold_metrics.pivot(index="fold", columns="model", values="rmse")
delta = wide.sub(wide["dummy"], axis=0)
```

13. 最直接做法是只创建一次 `cv_splits = list(cv.split(X))` 并把同一列表传给所有 `cross_validate`。
14. 在 `.agg` 中加入：

```python
rmse_min=("rmse", "min"),
rmse_max=("rmse", "max"),
```

15. 两个模型面对不同验证行，差异混合了模型与划分效应。

## D. 解读题

16. 不能。A 平均略低但波动更大；需要看逐折差值、异常折、样本量、分组合理性和应用对稳定性的要求。
17. 检查标签与特征对齐、数据质量、泄漏与分组、指标实现、模型配置是否适合，以及特征是否含有效信号。
18. 不能。小时间差受计时噪声、机器与实现影响，只能描述当前教程运行。
19. 示例：

> 五个固定基线在同一人工数据、同一 5 折和相同指标下比较，主指标为平均验证 RMSE。结果应同时读取逐折值与样本标准差，并以 Dummy 为最低参照。当前排序只说明本次教学协议下的观察，不支持 ESOL 或真实粘合剂上的普遍排名。
