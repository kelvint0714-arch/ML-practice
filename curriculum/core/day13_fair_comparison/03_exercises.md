# Day 13 练习：公平比较多个模型

## A. 概念题

1. 公平比较的“相同”至少包括哪六项？
2. Ridge 使用标准化、随机森林不使用标准化，是否必然不公平？
3. 为什么所有模型应复用同一批验证折？
4. Dummy 的作用是什么？
5. 平均 RMSE 最低是否证明模型普遍最好？
6. 为什么 Day 12 同折调出的最佳参数不能直接包装成 Day 13 的无偏性能？

## B. 表结构题

7. 5 个模型做 5 折，`fold_metrics` 应有多少行？
8. 写出逐折表至少七个字段及其含义。
9. 为什么 `model_summary` 不能替代 `fold_metrics`？
10. 某模型缺少第 4 折，但均值仍能算出。为什么必须把它判为不完整？

## C. 代码题

11. 在模型字典中加入 `Ridge(alpha=10)`，命名为 `ridge_alpha_10`，确认总行数变化正确。
12. 计算每个复杂模型相对 Dummy 的逐折 RMSE 差：

```text
delta = model_rmse - dummy_rmse
```

解释负值方向。
13. 检查每个模型的 `(fold, valid_indices)` 完全一致。提示：预先保存 `cv_splits`。
14. 在汇总表加入 `rmse_min` 和 `rmse_max`。
15. 故意让一个模型使用新的 `KFold(random_state=7)`，解释为什么即使折数相同也不公平。

## D. 解读题

16. 模型 A：`0.90 ± 0.30`；模型 B：`0.94 ± 0.05`。只凭这两个值能否断言 A 更可靠？还需查看什么？
17. 一个复杂模型平均 RMSE 比 Dummy 略差，应优先做哪些检查？
18. 小数据上拟合耗时相差 `0.001` 秒，能否写“模型 A 在所有硬件上更快”？
19. 写一段不超过四句话的合格结论，必须包含协议、主指标、波动和证据边界。

## 提交检查

- [ ] 所有模型共用相同 `cv_splits`；
- [ ] 需要学习的预处理都在 Pipeline 内；
- [ ] 负误差评分已恢复；
- [ ] 逐折表无重复、无缺折；
- [ ] 持久化汇总包含均值、标准差和折数，耗时只作 Notebook 现场诊断；
- [ ] 保留 Dummy；
- [ ] 没有使用测试集或夸大排名。
