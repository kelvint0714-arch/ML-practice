# Day 16 练习：MLP 前向传播

## A. shape 题

1. 输入 `(20, 10)`、隐藏层 8 个单元、单输出时，写出 `W1`、`b1`、`W2`、`b2` 的 shape。
2. 写出 `Z1`、`H1` 和预测的 shape。
3. `b1` 为什么能加到所有 20 个样本？
4. 若 `W2.shape == (7, 1)`，哪一步会失败？

## B. 概念题

5. 为什么多个纯线性层仍等价于一个线性变换？
6. ReLU 对负数、零和正数分别做什么？
7. 前向函数执行后权重会自动学习吗？
8. 为什么固定随机 seed 不代表随机预测具有性能证据？
9. 一个隐藏单元对所有样本的激活都为零，当前 trace 表会呈现什么？

## C. 手算题

10. 使用本讲义极小网络，把输入改为 `X=[[2, -1]]`，手算 `Z1`、`H1` 和预测。
11. 自己设计一个两输入、三个隐藏单元、一个输出的网络，只需写出所有 shape。
12. 给出一个 `Z1=[[-2, 0, 3]]`，写出 ReLU 输出。

## D. 代码题

13. 把 notebook 隐藏单元数从 4 改为 6，并更新所有断言。
14. 写 `identity(values): return values`，比较 ReLU 与恒等激活预测。
15. 在 trace 表增加“零值比例”，观察 ReLU 后零比例是否变化。
16. 保存所有权重副本，调用 `forward` 后检查完全不变。
17. 故意把 `b1` 写成错误长度，阅读广播异常或 shape 异常并解释。

## E. 表达题

18. 改写错误结论：

> 随机初始化 MLP 的预测看起来接近 y，所以神经网络已经训练成功。

19. 用三句话解释“前向、损失、优化”的顺序。

## 提交检查

- [ ] 所有参数 shape 在运行前写出；
- [ ] 能手算一个样本；
- [ ] trace 表包含每个中间量；
- [ ] 知道 ReLU 引入非线性；
- [ ] 权重未在前向中改变；
- [ ] 没有把随机输出当性能结果。
