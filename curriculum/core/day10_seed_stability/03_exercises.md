# Day 10 练习：多随机种子稳定性

## A. 概念题

1. 随机森林中至少有哪两类随机过程？
2. 固定 `random_state=42` 是否足以让实验在任意电脑、任意版本逐位一致？
3. 为什么不能只汇报八个 seed 中最好的一次？
4. 固定划分的多 seed 标准差能否称为“材料实验误差”？
5. 若每次 seed 改变时也改变 `n_estimators`，还能把波动归因于 seed 吗？
6. `mean`、`std`、`min`、`max` 各自提供什么信息？

## B. 设计题

7. 写出一个 6 个 seed 的预注册列表，并说明为什么在看结果前固定。
8. 为“只研究模型随机性”列出至少五个必须保持不变的条件。
9. 画一个 2×3 实验矩阵：2 个划分 seed 与 3 个模型 seed。说明它回答的问题与本日实验有何不同。
10. 如果某个 seed 的 RMSE 明显偏高，列出三项应先检查的内容，而不是直接删除该行。

## C. 代码题

11. 把 notebook 的 seed 列表改为 `[7, 11, 19]`，确认结果表恰好三行。
12. 增加 `prediction_mean` 列，记录每次验证预测的平均值。
13. 独立运行相同 seed 两次，计算预测的最大绝对差，并用 `np.allclose` 检查。
14. 故意把第二次运行的 `min_samples_leaf` 改掉。解释为什么此时复现检查失效。
15. 使用 `results["rmse"].idxmin()` 找到观察到的最低 RMSE 行，但写明为什么不能只保留这一行。

## D. 表达题

16. 补全规范句子：

> 在 ______ 划分、______ 个预先声明 seed 和固定 ______ 参数下，验证 RMSE 为均值 ± 样本标准差；该结果描述 ______，不描述 ______。

17. 改写错误结论：

> seed 42 最好，所以它是科学上最正确的随机种子。

18. 写出本日教学小数据结果不能外推到 ESOL或粘合剂任务的两个原因。

## 提交检查

- [ ] seed 列表在运行前写明；
- [ ] 除 seed 外参数完全一致；
- [ ] 每次运行都有一行原始记录；
- [ ] 汇总表没有替代原始表；
- [ ] 相同 seed 复现检查通过或留下完整失败记录；
- [ ] 没有使用测试集；
- [ ] 结论准确限定为模型随机性。
