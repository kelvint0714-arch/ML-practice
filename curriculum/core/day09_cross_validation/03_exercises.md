# Day 9 练习：K 折交叉验证

先完成题目，再打开 `04_reference_answers.md`。代码题可在当天 notebook 的末尾另建单元完成。

## A. 概念题

1. 5 折交叉验证一共拟合几个模型？每个样本当几次折内验证样本？
2. 为什么不能让第 2 折沿用第 1 折已经拟合的决策树？
3. 交叉验证的训练区域能否包含最终测试集？为什么？
4. 五折 RMSE 为 `[0.8, 0.9, 0.7, 1.8, 0.8]`。平均值是否足以描述结果？还应保存什么？
5. 普通随机 K 折为何可能高估对未见分子骨架的泛化？
6. 同一配方的三个重复试样被分到不同折，可能发生什么？

## B. 纸笔题

7. 12 个样本做 3 折且不打乱。写出每折验证索引，并说明每轮训练/验证行数。
8. 某折真实值为 `[2, 4]`，预测值为 `[1, 5]`。手算该折 RMSE。
9. 11 个样本做 5 折时，各折大小能否完全相同？最大折与最小折最多相差多少行？
10. 画一张数据边界图，必须出现“外部测试集封存”和“训练区域内部 K 折”。

## C. 代码题

11. 把 notebook 中的样本数改为 23，验证每个样本仍恰好成为一次验证样本。
12. 在逐折表中增加 `valid_y_mean`，观察较难折是否也有不同的目标分布。
13. 写一个断言，保证同一轮训练索引与验证索引没有交集。
14. 将 `shuffle=True` 改成 `False`。删除 `random_state` 后运行，并解释为什么 sklearn 不允许在 `shuffle=False` 时保留 `random_state`。
15. 用 `GroupKFold` 为下面的组号生成折，检查同一组是否跨折：

```python
groups = np.array(["A", "A", "B", "B", "C", "C", "D", "D", "E", "E"])
```

## D. 研究表达题

16. 改写下面的错误结论：

> 5 折平均 RMSE 很低，所以模型已证明能预测所有新分子。

17. 用两句话区分“随机 K 折稳定性”和“多随机种子模型稳定性”。
18. 写出把本日程序迁移到 ESOL 时必须记录的至少五项配置。
19. 写出当前人工数据实验“可以说明”和“不能说明”的内容各两条。

## 提交检查

- [ ] 有完整逐折表，而非只有均值；
- [ ] 有均值和标准差，并写明标准差口径；
- [ ] 有索引不重叠与覆盖次数断言；
- [ ] 没有使用测试集；
- [ ] 没有把随机 K 折称为 scaffold split；
- [ ] 没有把教学小数据写成 ESOL 或粘合剂结果。
