# Day 14 练习：传统机器学习阶段报告

## A. 审计题

1. 一个 RMSE 数字至少需要哪些上下文才能被复查？列出八项。
2. 为什么固定验证 RMSE 与 5 折平均 RMSE 不能直接求平均？
3. 为什么原始 logS RMSE 与标准化标签 RMSE 不可混合？
4. 逐折文件缺少一个模型的第 3 折，但汇总代码仍能运行，应怎样处理？
5. `report.md` 中的数字与 CSV 不一致时，应优先修改哪一层？

## B. 结构题

6. 为阶段报告写出十个一级或二级标题。
7. 分别写一句“证据”“解释”“决定”“边界”。
8. 写三条“可以得出”和三条“不能得出”。
9. 写一份缺失证据清单，至少五项。
10. 说明为什么进入 MLP 阶段不等于已经判断 MLP 会超过随机森林。

## C. 代码题

11. 为逐折表增加检查：所有 `rmse >= mae`。它在逐折聚合上通常应成立；若失败，先检查什么？
12. 检查每个模型的折集合都恰好是 `{1, 2, 3, 4, 5}`。
13. 将某一行 `rmse` 改为 `NaN` 的副本，验证审计代码会停止。
14. 计算每个模型相对 Dummy 的平均 RMSE 差，并保留原始单位。
15. 为汇总表加入 `source_file` 与 `protocol` 两列，增强可追溯性。

## D. 写作题

16. 改写错误句子：

> 随机森林赢了，所以下游任务应该直接使用随机森林。

17. 若 Day 10 没有实际运行，多 seed 部分应怎样写？
18. 若普通随机 K 折结果很好，报告中怎样说明 scaffold 泛化仍未知？
19. 写一个不超过 120 字的阶段摘要，明确这只是课程人工数据。

## 提交检查

- [ ] 所有数字都有源文件；
- [ ] 源文件通过 schema、重复、折数和有限值检查；
- [ ] 不同协议分开报告；
- [ ] 解释没有冒充证据；
- [ ] 未完成项明确写出；
- [ ] ESOL、人工数据和下游任务数据没有混称；
- [ ] 下一阶段的目的与公平比较规则清楚。
