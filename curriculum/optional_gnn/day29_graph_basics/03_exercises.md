# Day 29.3：图基础练习

先关闭 [参考答案](04_reference_answers.md)。先画图和手算，再运行代码。

## A. 名词对应

对下面每项写出它属于“节点、边、节点特征、边特征、整图标签”中的哪一种。

1. 玩具图中的 A；
2. A—B 的连接；
3. A 对应的 `[1.0, 0.0]`；
4. 假设整张图的目标值 `0.75`；
5. 分子图中的原子元素类型；
6. 分子图中的单键或双键类型。

## B. 手画与邻接矩阵

使用：

```text
节点：A、B、C、D
无向边：A—B、B—C、A—C、C—D
```

完成：

1. 给节点编号 0–3；
2. 手画图；
3. 写出无向边列表；
4. 手写 `(4, 4)` 邻接矩阵；
5. 检查矩阵是否对称；
6. 写出每个节点的邻居；
7. 写出每个节点的度数。

## C. 独立实现

不要复制 walkthrough，自己完成：

```python
def build_undirected_adjacency(number_of_nodes, edges):
    # 创建全零矩阵
    # 检查端点编号
    # 每条边写入两个方向
    # 返回矩阵
    pass
```

要求：

- 输出 dtype 是整数；
- 输出 shape 为 `(number_of_nodes, number_of_nodes)`；
- 邻接矩阵对称；
- 本练习拒绝自环；
- 节点编号超出范围时抛出 `ValueError`。

## D. 手算消息求和

继续使用 B 的图，并固定：

```python
node_features = np.array(
    [
        [1.0, 0.0],
        [0.0, 1.0],
        [1.0, 1.0],
        [2.0, 0.0],
    ]
)
```

完成：

1. 写出 `node_features.shape`；
2. 手算 A 收到的邻居特征之和；
3. 手算 C 收到的邻居特征之和；
4. 用 `adjacency @ node_features` 核对；
5. 写出输出 shape。

## E. Shape 解释

用完整中文句子解释：

1. 邻接矩阵 `(4, 4)` 的两个 4 分别是什么；
2. 节点特征 `(4, 2)` 的 4 和 2 分别是什么；
3. `degree.shape == (4,)` 为什么右边没有第二个数字；
4. `(4, 4) @ (4, 2)` 为什么能够相乘；
5. 输出 `(4, 2)` 为什么仍然有 4 行。

## F. Debug 题

下面代码想创建无向边 `A—B`，至少找出两个问题：

```python
adjacency = np.zeros((4, 4), dtype=int)
adjacency[0, 1] = 1

assert np.array_equal(adjacency, adjacency.T)
```

下面代码为什么不能直接表达“每个节点收到邻居的两个特征之和”？

```python
node_features = np.ones((3, 2))
messages = adjacency @ node_features
```

## G. 证据边界

用自己的话完成：

```text
今天的数据是什么：
节点编号是否是特征：
邻接矩阵表达什么：
邻接矩阵没有表达什么：
邻居求和是不是已训练模型：
今天的结果能支持什么：
今天的结果不能支持什么：
```

## 验收清单

- [ ] 能从图独立写出边列表和邻接矩阵；
- [ ] 能检查无向矩阵的对称性；
- [ ] 能解释度数和邻居；
- [ ] 能手算至少一个节点的消息和；
- [ ] 能解释所有关键 shape；
- [ ] 没有把玩具图当成材料研究结果。

全部完成后再打开：[折叠参考答案](04_reference_answers.md)。
