# Day 35 练习

先独立完成，不要先看答案。

## 练习 1：判断能否构图

对下列字段分别判断能否直接构造可信分子图，并说明原因：

1. `主树脂/基体名称 = 环氧树脂`；
2. `固化剂/交联剂名称 = 某商业牌号`；
3. 经化学组确认且带来源记录的 SMILES；
4. 一个配方中只有固化剂有 SDF，主树脂结构未知。

## 练习 2：区分空值和零

为什么空白模板中的“实测性能值”不能当成 0？错误地填 0 会对模型造成什么影响？

## 练习 3：图代表什么

写出至少三种可能的“图对象”，并说明为什么必须由课题问题决定，而不是由程序员任选。

## 练习 4：当前决定

根据以下事实作出 Go / 补数据后 Go / No-Go 决定：

- 0 行真实数据；
- 没有 SMILES/SDF 字段；
- 目标性能尚未冻结；
- 数据权限尚未确认；
- 只有课程中的公开图实验。

写出决定和至少四条依据。

## 练习 5：多组分

一个配方包含树脂 A、固化剂 B、填料 C、配比和两段固化程序。为什么只把 B 的分子图输入 GNN 不足以代表完整样本？

## 练习 6：公平比较

列出未来 GNN 与表格模型公平比较时必须保持相同的四项内容。

## 练习 7：数据交接

写一段发给化学组的请求，包含：

- 需要确认的样本定义；
- 结构格式与来源；
- 多组分对应；
- 首要目标和测试条件；
- 权限。

## 练习 8：代码理解

解释：

```python
structure_columns = [
    str(column)
    for column in data.columns
    if "SMILES" in str(column)
]
```

如果结果是空列表，它能说明什么、不能说明什么？
