# The core curriculum extends the tested ESOL environment only with plotting.
# GNN dependencies are intentionally excluded until the Day 28 gate.
-r requirements.txt
matplotlib==3.10.9
openpyxl==3.1.5
nbformat==5.10.4
nbclient==0.11.0
